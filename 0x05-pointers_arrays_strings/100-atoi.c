#include "main.h"
#include <stdlib.h>

/**
 * _atoi - a function that convert a string to an integer
 * @s: the string
 * Return: the number if exists, otherwise return 0
 */

int _atoi(char *s)
{
	int nb;
	nb = atoi(s);

	return (nb);
}
