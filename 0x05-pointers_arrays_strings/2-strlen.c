#include "main.h"
#include <string.h>

/**
 * _strlen -  a function that returns the length of a string
 * @s: the string
 */

int _strlen(char *s)
{
	int length;

	length = strlen(*s);

	return (length);
