#include "main.h"
#include <string.h>

/**
 * _strcmp - a function that compares two strings
 * @s1: a string
 * @s2: a string
 * Return: 0 if equals, >0 s1 greater, <0 s2 is greater
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
